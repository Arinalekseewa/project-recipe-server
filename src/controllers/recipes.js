export const createRecipeController = (req, res) => {
  res.json({ message: "createRecipeController заглушка" });
};

export const getAllRecipesController = (req, res) => {
  res.json({ message: "getAllRecipesController заглушка" });
};

// заглушка
