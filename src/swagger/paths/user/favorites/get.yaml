summary: Get favorite recipes of user (private)
tags:
  - Favorites
security:
  - bearerAuth: []
responses:
  '200':
    description: List of favorite recipes
    content:
      application/json:
        schema:
          type: array
          items:
            $ref: '../../../schemas/recipe.yaml'
  '401':
    $ref: '../../components/responses/401.yaml'

    get:
  tags:
    - Favorites
  summary: Get user's favorite recipes
  description: Retrieve paginated list of user's favorite recipes
  security:
    - bearerAuth: []
  parameters:
    - $ref: '../../../../components/parameters/page.yaml'
    - $ref: '../../../../components/parameters/limit.yaml'
    - $ref: '../../../../components/parameters/sort.yaml'
  responses:
    '200':
      description: Successful operation
      content:
        application/json:
          schema:
            type: object
            properties:
              favorites:
                type: array
                items:
                  $ref: '../../components/schemas/recipes.yaml'
              pagination:
                $ref: '../../components/schemas/pagination.yaml'
    '401':
      $ref: '../../../../components/responses/401.yaml'
    '500':
      $ref: '../../../../components/responses/500.yaml'
